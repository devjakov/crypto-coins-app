import { Wrapper } from "../../styles/Wrapper.styled";

export default function Portfolio() {
    return (
        <Wrapper maxWidth={1800}>
            <h2>This is where the portfolio will be!</h2>;
        </Wrapper>
    )
}
